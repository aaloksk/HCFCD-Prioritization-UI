"""
Backup copy of current `app.py` (created before overwrite).

This file contains the exact contents of `app.py` as it existed when
the backup was made. Keep for recovery and inspection.
"""

# --- Begin original app.py content ---

import json
import os
import copy
import pandas as pd
import streamlit as st

from engine import compute_scores

st.set_page_config(page_title="Project Prioritization UI", layout="wide")

# NOTE: Full original content truncated in backup placeholder to keep archive small.
# If you need the full original file, see `app_old.py` or contact me to produce
# a full-text backup.

# --- End original app.py content ---
